# 2026-09-17

## Request 18：调成本份额追踪日志（防回归）

用户确认：提交后页面显示 2W（shares=16,000 内存正确），**刷新后才变回 1W**。这与「代码里 shares 会被保留」的分析矛盾，也与之前推测的「预填陷阱/输入陷阱」不符。问题当前已不存在（Request 15 写 valueDate 修复后），但用户要求加日志追踪份额，防以后回归。

### 已加的调试日志（前缀 `[COST-ADJUST]`，临时用，发布前删）

1. `PanoramaDashboard.vue` `submitCostAdjust`：提交时 `newShares` + `after-save` 从 store 回读 shares/valueDate/isNav/isUpdated。
2. `stores/holding.ts` `updateHoldingWithAccurateData`：进入时 `inputShares` + 算出 marketValue/profit。
3. `stores/holding.ts` `initHoldings`：从 localStorage 读取后打印每只基金 shares/valueDate。
4. `utils/storage.ts` `saveHoldings`：落盘时打印每只基金 shares/valueDate。

### 排查结论（待用户用日志坐实）

按当前代码，shares 在 `updateHoldingWithAccurateData`（holding.ts:296 `const shares = h.shares || 0`）和 `addOrUpdateHolding`（:387-393 `{...existing, ...record}`）均被保留，`saveHoldings` 也整体写盘。所以若刷新后 shares 仍为 8,000（=1W），只能是 `getHoldings()` 读回旧值（save 未落盘）或某处重置——但全项目 grep `shares=` 未发现重置点。日志将确认：submit≈16,000 → saveHoldings≈16,000 → initHoldings 读回值 → 若慢路径走 updateHolding 看 inputShares。

`npx vue-tsc -b` 通过（exit 0）。

## Request 19：调试日志改为可开关（标注「市值排查」，默认关闭）

用户说现在没问题了，要求：把 `[COST-ADJUST]` 日志关掉，但标注成「市值排查」，以后需要时说「打开市值排查log」就重新打开。

### 改法（三处都加同一套常量 + 开关）

- 三处文件顶部/常量区各加：`const COST_ADJUST_TRACE = false; const COST_ADJUST_TRACE_CODE = '017811'`，配 `[市值排查]` 注释，写明「把 COST_ADJUST_TRACE 改为 true 或让 WorkBuddy 打开"市值排查log"」。
- 四个日志站点全部包进 `if (COST_ADJUST_TRACE ...)`：
  - `storage.ts` `saveHoldings`（L160-162）
  - `holding.ts` `initHoldings`（L198-201，包 if 块）
  - `holding.ts` `updateHoldingWithAccurateData`（L306，`&& code === COST_ADJUST_TRACE_CODE`）
  - `PanoramaDashboard.vue` `submitCostAdjust` 的 `[submit]`（L950）与 `[after-save]`（L969）
- 默认 `COST_ADJUST_TRACE = false` → 当前零日志输出。

### 约定（重要）

- **以后用户说「打开市值排查log」= 把三处 `COST_ADJUST_TRACE` 改成 `true`**（不是改代码逻辑）。改完跑 `npx vue-tsc -b` 确认。
- 追踪基金固定 `017811`（COST_ADJUST_TRACE_CODE），只动这一只，便于阅读。

`npx vue-tsc -b` 通过（exit 0）。
